function test(value: string): string {
    if (value === 'abc') {
        return 'abc';
    } else {
        return 'nothing';
    }
}

function add(a: number, b: number): number {
    return a + b;
}

function deleteIndex() {
    return void 0;
}